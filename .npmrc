# Force public npm registry for this project.
# Overrides any company-level .npmrc that points to a private registry.
# This ensures GitHub Actions (and any other CI) can install packages
# without needing corporate AWS CodeArtifact credentials.
registry=https://registry.npmjs.org/
